### 🛠️ Prompt 6.5: "The core Engines (Allocation & FTP)"

> "The service shells and stored procedures are in place. Now we must implement the 'Powerhouse' of the application—the core calculation engines.
> 
> 1. **Allocation Engine**: Build `app/services/allocation_engine.py`. This is our most critical service. It must implement the logic described in the **README.md (Allocation Engine section)**:
>    - Perform the configuration-driven Pandas joins between source and lookup tables.
>    - Implement the three allocation methods: `RATIO`, `DISTRIBUTION`, and `STATIC`.
>    - Crucially, implement the **Financial Element Unpivot** logic. If the `OutputModel` supports `financial_element`, the engine must structurally multiplex the balance into `BAL`, `II`, and `COF` rows.
>    - Ensure the engine is idempotent, correctly handling existing data for the same as-of date.
> 
> 2. **FTP Engine**: Build `app/services/ftp_engine.py`. This must follow the **Component Architecture** specified in the **README.md (Fund Transfer Pricing section)**:
>    - Implement the `MOVING_AVG` calculation method with lookback windowing.
>    - Independently calculate `COF`, `LP`, and `CLP` components per instrument.
>    - Write results back to the processing tables as specified.
> 
> 3. **Data File Service**: Build `app/services/datafile_service.py` to handle the batch file I/O engine:
>    - Support both `fixed_length` and `delimited` formats using the JSON rule definitions.
>    - Implement the **Transform Expression Sandbox** allowing for safe-eval of functions like `lpad`, `iif`, and `to_float`.
> 
> Refer strictly to the **Architectural Governance** and **Service Layer Sovereignty** sections in our documentation. These engines must be high-performance, idempotent, and fully traceable."

***

### Why this is the vital 6.5 Step:
While Step 6 builds the "shells," Step 6.5 builds the "Brains." By isolating the computational logic of the Allocation and FTP engines into their own implementation phase, you ensure the AI focuses entirely on the mathematical accuracy and data shredding logic without getting distracted by UI or routing concerns. Referencing the `README.md` at this stage is crucial because it contains the physical mapping and multiplexing rules that define the platform's financial sophistication.
