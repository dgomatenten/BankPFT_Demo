[pytest]
markers =
    ui: Selenium browser UI tests (require webdriver-manager and chromedriver)
    integration: PostgreSQL integration tests (require DATABASE_URL pointing to a live PostgreSQL server)
